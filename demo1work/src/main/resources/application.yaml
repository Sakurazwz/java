spring:
  application:
    name: profile-demo
  profiles:
    active: dev

datasource:
  url: jdbc:mysql://localhost:3306/profile_demo
  username: root
  password: root

management:
  endpoints:
    web:
      exposure:
        include: health,info,env,refresh

# 主配置文件
spring:
  application:
    name: demo
  # 激活的 Profile（默认为 dev）
  profiles:
    active: dev

# 应用端口
server:
  port: 8080

# 应用信息（用于文档显示）
app:
  name: ${spring.application.name}
  version: 1.0.0
  description: Spring Boot 示例项目
